[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-lab"
version = "0.1.0"
description = "Alpha48Alpha AI Lab — RL, world models, and creative AI systems"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["ai_lab*"]
